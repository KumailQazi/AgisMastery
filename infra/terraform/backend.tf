# Optional: Google Cloud Storage (GCS) Remote State Backend
# Uncomment and configure to enable team-based state locking

# terraform {
#   backend "gcs" {
#     bucket = "your-gcp-project-id-terraform-state"
#     prefix = "mastery/state"
#   }
# }
